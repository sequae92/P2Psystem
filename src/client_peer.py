# client_peer.py
# Implement rfc_query and get_rfc functions.
# Store a list of RFC index values.
# Each client will have its own index list, class variable.
# 



class Peer_Client:
    #  

class Index:
    i.rfc_num, i.rfc_title, i.peer_hostname
    def __init__(self, rfc_num, rfc_title, peer_hostname):
        self.rfc_num = rfc_num
        self.rfc_title = rfc_title
        self.peer_hostname = peer_hostname
